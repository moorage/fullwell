# Verification Evidence

## Public support and privacy contact correction

Date: 2026-07-27

- The shared public brand contract, homepage/company/legal rendering, Privacy Notice, Terms, client product specification, browser assertions, and prior identity-release record now use `support@fullwell.ai` and `privacy@fullwell.ai`. Repository-wide source search finds no stale support or privacy `.app` contact.
- Focused acceptance passes 61 web tests, web typecheck, the production web build, and four WebKit identity checks across desktop, iPhone 13, 320x568, and no JavaScript. Full repository verification passes lint, typecheck, production builds, 421 application tests with 11 expected database-gated skips, docs verification, and active-ExecPlan verification.
- Release commit `1a74fc170d0f0af8c7ee7a1c6e3bd403e4121976` is pushed. DigitalOcean runs healthy Linux/amd64 image `hfj-staging:contact-domains-20260727-1-runtime` at OCI index `sha256:1f4849860582a8d53fe011e96172f87a3cfdbbbf0949dd220afde07c7f255555` and concrete manifest `sha256:151a157da4363a76f58a174efd47988590871d59901e43c8ede89ca877994581`. Its 81,012,736-byte archive matched locally and remotely at SHA-256 `2a86eec6d0d16974e044eadea906401594d2ee2601ba1557d3eb0da4b198c7eb`.
- Production server-rendered HTML and live WebKit checks pass on `/`, `/company`, `/privacy`, and `/terms`: every public support/privacy link uses the exact `.ai` address and matching `mailto`, with no stale `.app` contact. Deployment and MCP discovery smokes pass, and public readiness remains healthy on schema `0008`.
- `/etc/hfj/deploy.env.pre-contact-domains-20260727-1` and `hfj-staging:character-artwork-20260727-1-runtime` retain rollback. No migration, household Git mutation, Caddy/provider/secret change, or npm publication occurred. The transferred archive and temporary local build context were removed.

## Fullwell character artwork

Date: 2026-07-27

- The canonical homepage hero uses the exact supplied `774x1247` full-body PNG, and the shared masthead uses the exact supplied `454x455` face PNG beside the visible `Fullwell` wordmark. Both are decorative, have explicit intrinsic dimensions, preserve the existing product and install flow, and add no browser authority or data/provider change.
- Source and checked-in SHA-256 values match: full body `992d1d3a81d36a6d2b1a2e74a55d855557cdab3972fcb5cc1403f6ccc0c31219`; face `c03c30d5d13f7f74f6e4887805ffc659f317c2def2709c090081bd62bdb5fa04`.
- Focused acceptance passes 61 web tests, web typecheck, the production web build, and eight targeted WebKit checks. The complete browser gate passes 142 checks with 22 intentional project skips; the dedicated accessibility gate passes six applicable checks with six intentional project skips. Full repository verification passes lint, typecheck, production builds, 421 application tests with 11 expected database-gated skips, docs verification, and active-ExecPlan verification.
- Reviewed Playwright screenshots at 1440x900, iPhone 13, 320x568, and no JavaScript show no horizontal overflow. The desktop agent chooser remains in the first viewport, and the bounded mobile character does not obstruct the existing install controls.
- The repository screencast helper was attempted for `artifacts/screencasts/fullwell-character-artwork.mp4`, but Homebrew FFmpeg 8.0.1 rejected the unavailable `x11grab` input and exited 234 before capture. No partial MP4 was produced; the reviewed WebKit screenshots are the local visual evidence.
- Release commit `6fffbe826f456a86804336f1483ad5fee5d27da2` is pushed. DigitalOcean runs healthy Linux/amd64 image `hfj-staging:character-artwork-20260727-1-runtime` at OCI index `sha256:1a58fc469607d958992a8d108e1a6f7ea57c393f7de965a257d715b522d3e950` and concrete manifest `sha256:7da109a704de8a3f930b5e21461dd74eb0ca65222c9003c3b30020a4f08d0e4d`. Its 81,012,736-byte archive matched locally and remotely at SHA-256 `f1e3f27e471aa50d16b5b59ceef48925773f9536a96df911dbc6ca027ff47fbb`.
- Production serves both PNGs with their exact source SHA-256 and `image/png`; initial HTML references both assets. WebKit screenshots at 1440x900, iPhone 13, and 320x568 are visually accepted with no clipping, overflow, or displaced install controls.
- Apex liveness/readiness, permanent path-and-query redirects, Apple authorization generation with the exact apex callback, deployment smoke, MCP discovery, non-destructive signed WhatsApp handling, and mounted-volume persistence across service recreation pass. Authenticated operator readiness, reconciliation, backup, repository, restore, signing, and volume checks are healthy; aggregate status remains degraded only by the pre-existing one response-ready message while no runner is online.
- `/etc/hfj/deploy.env.pre-character-artwork-20260727-1` and `hfj-staging:canonical-origin-20260727-1-runtime` retain rollback. No migration, household Git mutation, Caddy/provider/secret change, or npm publication occurred. The transferred archive and temporary persistence canary were removed.

## Canonical origin migration

- 2026-07-28 local follow-up accepts the merged ChatGPT desktop client's bounded `http://127.0.0.1:<port>/callback/<nonce>` redirect in the consent-only CSP and renames the hosted MCP declaration plus initialize metadata to `fullwell-cloud`. Focused server, 53-test dual-host package, 14-test eval, 14-test security, full repository verification with 423 passing application tests and 11 expected database skips, and WebKit E2E with 142 passes and 22 intentional skips all pass. Package `1.1.19` is prepared but not published; the server change is not deployed, and live merged-desktop reconnect evidence remains pending.
- 2026-07-27 local implementation changes the sole application and packaged-client origin to `https://fullwell.ai`, adds final redirect-only handling for `www` plus `fullwell.souschefstudio.com`, and adds a separate exact-path WhatsApp webhook transition configuration. Caddy 2.10 validates both configurations in Apple Container. Focused gates pass 2 Caddy tests, 12 server tests, 61 web tests, 53 package tests, and package validation for 9 skills, 42 tools, and 182 eval cases.
- The complete local gate passes lint, typecheck, build, 421 application tests with 11 expected database-gated skips, 14 security tests, 142 WebKit E2E checks with 22 intentional project skips, 6 accessibility checks with 6 intentional project skips, and the exact 90% branch-coverage threshold. `npm run verify`, docs verification, and active-ExecPlan verification pass.
- The planned migration screencast failed before capture because Homebrew FFmpeg 8.0.1 does not provide the helper's `x11grab` input; FFmpeg exited 234 and no partial artifact was created. Live browser verification and the automated production evidence below remain authoritative.
- The 10,000-record delivery contract case received a test-local 15-second timeout after whole-repository coverage instrumentation repeatedly exceeded Vitest's generic five-second limit; the unchanged deterministic assertion completes in about 1.4 seconds alone and 5.7 seconds under coverage.
- After explicit action-time confirmation, the existing Fullwell Apple Services ID saved and reloaded `fullwell.ai`, `fullwell.souschefstudio.com`, and the exact HTTPS `/auth/apple/callback` return URL on each host. No Services ID, App ID association, or key was created, removed, downloaded, or rotated.
- Release commit `48048ba3ce3d82175a6abed1bd54a87867dda24e` is pushed. Public immutable `@fullwell/fullwell@1.1.18` has matching prepared and registry SHA-1 `9a571b41655d20ccbf24e544a69016dfb8b1cf18`; the 33-file package passes clean Codex and Claude host lifecycles.
- Before the application cutover, the temporary gateway served only the exact apex WhatsApp GET/POST path. Meta then saved and reloaded `https://fullwell.ai/api/messaging/whatsapp/webhook`, mTLS off, with only `messages` subscribed at Graph API v25.0. Verification, invalid-token rejection, unsigned rejection, and signed empty delivery pass without a redirect. Beads bug `fullwell-z0t` separately tracks rotation of the existing verification token after the provider UI exposed the field through accessibility tooling; no value is recorded here.
- DigitalOcean runs healthy Linux/amd64 image `hfj-staging:canonical-origin-20260727-1-runtime` at OCI index digest `sha256:8f60ab4ef2ca8fe3bd20bbbf2798f2604c6ed4d46cc565942d6a887affa3c8e1` and concrete amd64 manifest `sha256:1441e7c2ede5cd4ecaa7ffe129ff31eb06faf02498d03eb141e7372a33256a47`. The 79,073,792-byte archive has SHA-256 `8c8d6d4231d289de4ad5ed4051a30c591b1d82dfccb7f66e607d9588e4cf32d6`.
- The deployed Caddyfile has SHA-256 `8a8dc22e1dc727fd42f1146312aa44a426161a6a93dc3b1ac9b9e513e95bd1eb`; `PUBLIC_DOMAIN=fullwell.ai`. The apex home, liveness, and readiness return `200`; `www` and the legacy host return exact path-and-query-preserving `301` redirects. Deployment and MCP discovery smokes pass, and OAuth metadata advertises only apex endpoints.
- A real Apple passkey authorization used client ID `com.souschefstudio.fullwell.web` and exact callback `https://fullwell.ai/auth/apple/callback`, returned to the authenticated apex households page, and remained authenticated after reload. No Apple credential was exposed or downloaded.
- The mounted-volume persistence canary round-trips and is removed. Operator health reports zero incomplete mutations and zero quarantined households; its aggregate remains degraded only because one previously known response-ready WhatsApp record has no online runner.
- `/etc/hfj/deploy.env.pre-canonical-origin-20260727-1`, `/opt/household-food-journal/deploy/Caddyfile.pre-canonical-origin-20260727-1`, and prior image `hfj-staging:household-rename-20260727-1-runtime` retain rollback. No database migration or household Git mutation ran. Email/Fullwell-passkey checks and installed-client reconnects remain to be recorded if needed.

## Household naming on web and chat

Date: 2026-07-27

- Release commit `1605e3a` is pushed to `origin/main`. Household owners can rename a connected household from a hover-, focus-, and touch-accessible title control whose native dialog autofocuses and selects the current name; the server delegates to the existing owner-only, CSRF-protected, idempotent, exact-HEAD `hfj_update_household_name` path used by chat. Editors and viewers receive no control or authority, and the initial HTML retains a direct no-JavaScript form.
- Local acceptance passes 98 web tests, 25 focused server tests, 14 agent evals, 142 applicable WebKit checks with 22 intentional project skips, Browser-plugin guide inspection, zero production dependency vulnerabilities, production build, and full verification with 420 application tests and 11 expected database skips.
- DigitalOcean staging runs `hfj-staging:household-rename-20260727-1-runtime` at Linux/amd64 OCI index digest `sha256:505143d96320bd6be429809328858042323ebbacd33a52a483d7f1bb691ba0f0` and concrete amd64 manifest `sha256:2c2d93f6eabf8e0dd5535850efa9a0a88d0915d5ce63a9fc8c91cf5ef1b8836b`. Its 79,073,792-byte archive has SHA-256 `103907bc606e638cd0e63876c4d054138532b6d41f317208bfe77455f6d3d9dd`, matched locally and on the Droplet before activation.
- Public readiness reports schema `0008`; deployment and MCP discovery smokes pass; `/guides/household-name` serves the chat and website instructions; a fully parsed anonymous rename request returns `401` without mutation; the mounted-volume canary round-trips and is removed; and the running container reports healthy on the exact immutable image.
- `/etc/hfj/deploy.env.pre-household-rename-20260727-1` and `hfj-staging:durable-images-20260727-1-runtime` retain rollback. The transferred archive was removed after activation. Authenticated operator readiness, reconciliation, backup, repository, signing, volume, and restore checks pass; aggregate status remains degraded only because one previously known WhatsApp response is waiting while no runner is online.

## Durable computer-use images

Date: 2026-07-27

- Authorized grocery, recipe, and delivery computer-use audits now retain exact visible image/page provenance when available and commit it through the existing local or hosted journal mutation. History-backed delivery dishes preserve those fields through strict contracts, local validation, Git reconstruction, Takeout, selected public-safe collections, and import; legacy delivery records default both fields to null without a format bump.
- The image-capture change passes 416 deterministic application tests with 11 database-gated skips, 53 packaging tests, 14 eval tests, 14 security tests, 43 applicable focused WebKit checks with 9 intentional project skips, lint, typecheck, production build, and enforced coverage. The standard macOS screencast helper failed on unavailable `x11grab` with exit 234; the Playwright-native fallback produced a verified 4.12-second 1440x900 H.264 fixture recording at `artifacts/screencasts/durable-computer-use-images.mp4`.
- Release commit `13f82ca` is pushed. Public immutable `@fullwell/fullwell@1.1.17` is npm `latest`; registry SHA-1 `affc021c95e2017be63cee61a73292c9bfe465f3` and SHA-512 `sha512-9hA2VuY1kKGLIGXiqQkVZwelcAbPwXw75RxGNVuZbHF6Ss+IzfI/bgIoGPv48lF7KWzeOzAZHdjdAKIjW246WQ==` match the prepared 33-file artifact, and a clean registry download passes isolated Codex and Claude lifecycles.
- DigitalOcean staging runs zero-production-vulnerability Linux/amd64 image `hfj-staging:durable-images-20260727-1-runtime` at OCI index digest `sha256:e2593a7145c71a64c0ae59135ef4c4b33d3d6b736da8f00a80fe5ad58b5dc287` and concrete amd64 manifest `sha256:6ae850bfd126ffcfb0282157ed4b365759f83c8a263729496203ba70c3762caa`; its checksum-matched 79,069,696-byte archive has SHA-256 `97b45643255204c0bafc0a4e247fd89df45911a5ddd9c6b0dc3171302b09c991`.
- The first activation preserved and restored the prior deployment when Docker 29 treated Apple's imported unqualified OCI-index tag as unavailable to Compose. Retagging the reviewed digest through a Docker-normalized base reference made platform inspection and a bounded Node runtime canary pass before the successful retry. Public readiness reports schema `0008`; deployment, MCP discovery, mounted-volume persistence, deployed release `1.1.17`, and permanent alias redirect checks pass.
- `/etc/hfj/deploy.env.pre-durable-images-20260727-1` and `hfj-staging:public-brand-20260727-1-runtime` retain rollback, and the transferred archive was removed after activation. Authenticated operator readiness, Git, volume, signing, schema, single-writer, backup, repository, and restore checks pass. Aggregate operator status remains degraded by one incomplete mutation that predates the first deployment restart and the previously known response-ready WhatsApp record with no online runner; neither condition makes deployment readiness fail.
- No live provider, private household backfill, or cloud sync was part of this release. Existing journal records remain unchanged until the member authorizes their normal browser audit or refresh.

## Delivery cloud commit capacity

Date: 2026-07-26

- Commit `b6a57d9` is pushed to `origin/main`. The delivery cloud contribution contract accepts up to 10,000 completed evidence records and 10,000 delivery dishes per provider request, rejects 10,001, and retains the independent 16 MiB request bound.
- Contracts build, 52 package tests, 408 application tests with 11 expected database-gated skips, full repository verification, documentation verification, ExecPlan verification, and the pruned production dependency audit pass.
- DigitalOcean staging runs `hfj-staging:delivery-limits-20260726-1-runtime` at Linux/amd64 OCI index digest `sha256:a6d08842bfbdd1e5f78f2047077fd10c8145d87354c3d7110362bf1dc23ec8ab` from checksum-matched archive SHA-256 `e9c0df1afae1d65a52621a19f9c78a0e352d1d699c6cf8cc5645461819924cc0`. Public readiness reports schema `0008`; deployment and MCP discovery smokes pass.
- Authenticated operator health reports healthy readiness, reconciliation, backup, repository, signing, and volume checks. Its aggregate status remains degraded because one five-day-old response-ready messaging record has no online runner; no current deployment check failed.
- `/etc/hfj/deploy.env.pre-delivery-limits-20260726-1` retains `hfj-staging:takeout-browser-20260726-1-runtime` as rollback. The transferred archive was removed after activation.

## Food-delivery Milestone 7

Date: 2026-07-26

- The authenticated household website now exposes a `Takeout` tab and Journal-at-a-glance count. Same-name restaurant locations remain separate; cards distinguish ordered-before from shared dishes, delivery from pickup, and food from alcohol while repeating the structural manual-checkout boundary.
- JavaScript infinite loading appends two deterministic 12-item cursor pages from a 25-item fixture, deduplicates IDs, retains retry/end states, and binds every continuation to the initial Git HEAD. A concurrent HEAD change returns a refresh-required conflict. The ordinary `?page=N` link remains the keyboard and no-JavaScript fallback.
- Server and web tests cover strict runtime parsing, 40- and 64-character Git object IDs, authorization, stale snapshots, public-import authority, canonical evidence narrowing, exact location ordering, private-field exclusion, overview navigation, and the unchanged `/meal-plan` route behind the `Meal plans` label.
- Focused WebKit evidence passes 39 checks with 9 intentional project skips across desktop, mobile, 320-pixel, no-JavaScript, axe, reduced-motion, and a hydrated two-page automatic append. The post-polish visual-journal matrix passes all four projects.
- Deterministic coverage passes 408 tests with 11 database-gated skips at 96.49% statements/lines, 95.71% functions, and 90.05% branches. Contract/packaging, security, eval, load, eight-migration up/down/up, and all 11 Apple Container PostgreSQL integration tests pass.
- The repository ffmpeg helper failed early because the macOS build has no `x11grab` input. The documented Playwright-native fallback produced `artifacts/screencasts/food-delivery-takeout-browser.mp4`, a 10.52-second, 422,921-byte H.264 1280x800 fixture-only capture of overview navigation, the Meal plans/Takeout tabs, Palo Alto and Cupertino Wanpo cards, and two automatic append steps. It contains invented data and never opens a provider or checkout.
- Commit `6350469` is pushed to `origin/main`. DigitalOcean staging runs `hfj-staging:takeout-browser-20260726-1-runtime` at Linux/amd64 OCI index digest `sha256:93f6df13dee5b0f1dec736325a3675163e0a8f5a70470128ed7460b1a5052e0a` from checksum-matched archive SHA-256 `93eb847df3370083957e1902728a5bb6d269eaccb32b1fd44b1e166e5690e2d6`. The image carries the exact Git revision and has zero production dependency vulnerabilities. Public readiness and operator health pass on schema `0008`; deployment and MCP discovery smokes pass; the anonymous Takeout route preserves the encoded sign-in return path; and a mounted-volume canary survives forced service recreation. `/etc/hfj/deploy.env.pre-takeout-browser-20260726-1` retains `hfj-staging:food-delivery-20260725-1-runtime` as rollback, and the transferred archive was removed after activation.

## Food-delivery Milestones 1-6

Date: 2026-07-26

- Deterministic provider-neutral evidence covers complete and incomplete order groups; exact provider, restaurant location, fulfillment, dish, modifiers, and quantities; provider-then-location ambiguity; provider-scoped household contribution and recovery; public-safe collection/import authority; local/cloud delivery-dish meal proposals; full-cart proof, preservation, replacement confirmation, missing-delta recovery, ordinary-maximum alcohol selection, user-controlled age UI, prompt-injection isolation, and structural no checkout.
- Milestone 3 root validation passed 38 contract, 47 packaging, 14 eval, 12 security, and 72 browser cases plus typecheck, scoped lint, and 91.63% contract-domain branch coverage.
- Milestone 4 validation passed 39 contract, 47 packaging, 45 focused server, 84 web, 13 security, 14 eval, 32 browser, 6 accessibility, and 75 recovery cases plus build/type/lint/coverage gates.
- Milestone 5 validation passed 39 contract, 49 packaging, 89 focused server, 84 web, 14 security, 14 eval, and 12 browser cases plus build/type/lint/coverage gates. The contract workspace build preceded dependent server/web verification.
- The privacy boundary excludes credentials, raw pages, delivery destinations, payment/account data, private provider/order/menu/merchant locators from public snapshots, and all age/identity documents. Delivery telemetry is constrained to non-content outcome classes. Provider-origin revocation blocks future reads without claiming erasure of canonical contributions.
- Rollout remains gated in this order: provider-neutral index/promotion, fixture cart preparation, one authorized live provider canary, then a second provider. Rollback disables cart mutation before indexing, preserves additive canonical Git history, discards only ephemeral plans, and rebuilds/removes only noncanonical projections.
- Milestone 6 local release hardening passes lint, typecheck, production build, contract, integration, migration, security, eval, load, coverage, and full WebKit gates. The browser matrix passes 135 checks with 13 intentional project skips; Apple Container PostgreSQL passes all eight migrations up/down/up plus 11 integration checks; `npm audit --omit=dev` reports zero production vulnerabilities; and the pinned Node 24 Apple Container image builds successfully.
- `artifacts/screencasts/food-delivery-cart-preparation.mp4` is a 6.68-second, 156,794-byte H.264 fixture-only artifact assembled from Playwright-native recordings. It shows provider choices, two invented Wanpo locations, the exact Stanford-versus-Cupertino ambiguity, Stanford selection with the prior coconut line visible, the verified wintermelon cart with unrelated lines preserved, an explicit manual-checkout stop, a public-safe delivery collection, and a delivery-dish meal-plan view. The repository helper could not use its Linux X11/PulseAudio capture inputs on this macOS host, so Playwright-native video was the approved local fallback.
- Commit `65aca44` is pushed to `origin/main`. Staging recorded a root-only schema `0007` checkpoint at `2026-07-26 05:11:32.773411+00`, LSN `0/4416FE8`, then applied reversible migration `0008` through the exact-host direct-endpoint runner.
- DigitalOcean staging runs `hfj-staging:food-delivery-20260725-1-runtime` at Linux/amd64 OCI index digest `sha256:8bfdb7c161d96b72b19bcf60e5d8678c51de03f9965b14f2b70d3f517d578070` from checksum-matched archive SHA-256 `53d4da715422e9091072449c96d090043bc61724fc63c0eaea632118ee602f5a`. Readiness reports schema `0008`; the pruned runtime audit reports zero vulnerabilities; public deployment and MCP discovery smokes pass; `/etc/hfj/deploy.env.pre-food-delivery-20260725-1` and the root-only checkpoint retain rollback evidence.
- Public immutable `@fullwell/fullwell@1.1.14` was published as npm `latest` at `2026-07-26T05:13:25.261Z`. Registry SHA-1 `a1b7b7a59ca7b6d3fb9d3d7a209237d73e9aabd3` and SHA-512 `sha512-wanccwwIH7Ru6pkERPVlyt3uMOkoFrHV3259fJl38Y7GpssShPLq9idYUgfF1baTTxoEjD3+LXhcVHmmhszy5g==` match a clean 33-file download, and isolated Codex plus Claude lifecycle tests pass against that downloaded artifact.

This evidence does not establish live DoorDash, Uber Eats, or additional-provider support. No authorized live provider run, current Codex/Claude installed-host live-provider run, live alcohol age-step proof, or manual privacy/accessibility approval was performed for this change. Named providers, including their alcohol sub-capabilities, are therefore unsupported for release until an authorized no-checkout run proves otherwise. The provider-neutral package and server release do not enable checkout, and no checkout was opened or attempted.

## Prior release evidence

- Public immutable `@fullwell/fullwell@1.1.12` was published at `2026-07-24T16:28:33.419Z` with registry SHA-1 `1b80bf33aced31dafe9bea2564af1b03a53909f2` and SHA-512 `sha512-IPi/bY2m+rLhIgO0h1BxDhxNYzN7R7U216TOnt57h6btzvnMcszibavAVtuNvZHbl4YBjel5gOiK85FaOHdE/Q==`, byte-for-byte matching the prepared 28-file artifact. `latest` resolves to `1.1.12`; a clean registry install passes isolated Codex and Claude install, update, connection, disable, re-enable, and removal lifecycles. The release adds name-first identity and household naming, conversational local controls, context-sensitive invitation and collection examples, collaborative meal planning, a private recipe board, and an optional host-native weekly planning check-in.
- Commit `681952e` is pushed to `main`. Whole-grocery onboarding passes 300 deterministic tests with 11 expected database-gated skips, 39 Codex/Claude eval cases, 29 WebKit checks with seven intentional skips, and the contract, security, packaging, documentation, and full repository gates.
- Staging runs `hfj-staging:whole-grocery-20260722-1-runtime` with Linux/amd64 OCI index digest `sha256:a18a9bdffeb4869d0e4c499755a1cfa4fe619c0137cf2e5df3895ae73fd5a641` from checksum-matched archive SHA-256 `44524cd4ef00b7d271c2110c598e341f3f8498cea9cdc783d03ab99d91616441`. Public readiness, deployment, and MCP smokes pass at schema `0007`; `hfj-staging:onboarding-capacity-20260722-2-runtime` and `/etc/hfj/deploy.env.pre-whole-grocery-681952e` remain available for rollback.
- Public immutable `@fullwell/fullwell@1.1.7` was published at `2026-07-22T22:26:32.053Z` with registry SHA-1 `8dadf6a576cc47adc8841bc3cb7aaa3f72ddac7b` and SHA-512 `sha512-pacGSP9DQCLJSm4EYIRNxw3zzIFh6VbdTfnDbWpBk1+m3vhdrhaeK2VoXsHkuaYJjdt+cCfvpsWjD60JRfgt8g==`, matching the prepared 18-file artifact. `latest` resolves to `1.1.7`, a clean registry install passes isolated Codex and Claude lifecycles, and both current hosts report enabled `fullwell@fullwell` version `1.1.7`.
- On 2026-07-24, Namecheap authoritative DNS returned `the application gateway address` for both `fullwell.ai` and `www.fullwell.ai`; the separate email-forwarding TXT record was left unchanged. Caddy completed distributed ACME HTTP-01 validation and obtained valid certificates for both aliases. HTTP returns `308` to the same alias over HTTPS, and HTTPS returns `301` to `https://fullwell.souschefstudio.com` while preserving the tested household path and query. The canonical deployment smoke passes, `/account` still sends anonymous users to canonical sign-in, and all advertised OAuth issuer and endpoint URLs remain canonical.
- Commits `b6b12db` and `785ec06` are pushed to `main`, with the latter refreshing only the transitive `fast-uri` releases affected by GHSA-v2hh-gcrm-f6hx. `npm audit --omit=dev` reports zero production vulnerabilities, full verification passes 284 tests with 11 expected database-gated skips, and WebKit passes 29 checks with seven intentional skips.
- Staging runs `hfj-staging:onboarding-capacity-20260722-2-runtime` with Linux/amd64 OCI index digest `sha256:87cc996a1c1d2666ccf1700cb9052655e24d937e1f1a6b8b3d9f9fafe21cad65` from checksum-matched archive SHA-256 `f433edbee1f69dde9e88017be9cfaae56bd94aaaf68e1654e5eeeb3cb2dcd898`. Public deployment and MCP discovery smokes pass at schema `0007`; an unauthenticated 1,100,070-byte MCP request reaches authentication with `401` rather than the former parser `413`; and `hfj-staging:onboarding-drafts-20260722-1-runtime` plus the pre-release environment file remain available for rollback.
- Public immutable `@fullwell/fullwell@1.1.6` was published at `2026-07-22T20:55:57.184Z` with registry SHA-1 `1bb746d86d651176aa8cdab13ceff11e3655043d` and SHA-512 `sha512-P0tKysnAyMgycArCGRkk//4HNN83w7J7a5DPqtiaMFhwxLs8ixLtjksifZeqD+ZfInlANTInJlZB+7M0EZCtjA==`, matching the prepared 18-file artifact. `latest` resolves to `1.1.6`, a clean registry install passes isolated Codex and Claude lifecycles, and both current hosts report enabled `fullwell@fullwell` version `1.1.6`.
- Commit `917c7bc` is deployed as `hfj-staging:onboarding-drafts-20260722-1-runtime`. The checksum-matched Linux/amd64 OCI archive has SHA-256 `569952d091e96e3dba0503a534580f1d497280be539f7d2eefe5297c93bc7ae3`; its OCI index digest is `sha256:b88f85a3c3108708498e46275181d7b75ec68f5254db86b39213c30c9abf25b0`. Public live/readiness, deployment, and MCP discovery smokes pass at schema `0007`, and `hfj-staging:onboarding-20260721-1-runtime` remains available for rollback.
- The read-draft-commit release passes 279 deterministic tests with 11 expected database-gated skips, 11 isolated PostgreSQL tests, seven-migration up/down/up, 29 WebKit checks with seven intentional project skips, 35 Codex/Claude eval cases, eight package/lifecycle checks, security/load/contract gates, and 96.47% statement/line, 94.85% function, and 90.03% branch coverage. The final write is bounded, replay-bound, recoverable, and returns derived onboarding state without a follow-up read.
- Public immutable `@fullwell/fullwell@1.1.5` has registry SHA-1 `2142c869f01933a79dc458aa137136a7ca6f584a` and SHA-512 `sha512-aizf/lfRTYIcFHDD7mfXK89Q9I0vcNcPkl+QzDy6CbkoMi0da3s4gfBtg9oswUoMRRByBgvCfUNDXrnQemuoyg==`, matching the prepared artifact. `latest` resolves to `1.1.5`, and the downloaded package passes isolated Codex and Claude lifecycles plus local helper tests.
- Current Codex and Claude both report enabled `fullwell@fullwell` version `1.1.5`. Fresh `gpt-5.6-sol` sessions from the separate `fullwell-tester` folder saved a context-bound checkpoint, resumed and deleted its exact revision, and verified the exact shard missing afterward; every retained result contains only booleans and a zero Fullwell mutation count.

Date: 2026-07-22

## Passing local evidence

- Conversational Fullwell onboarding passes the typed 23-tool contract, 30-case Codex/Claude eval matrix, real isolated Codex and Claude plugin lifecycles, per-user memory/Neon compare-and-set tests, service idempotency/authorization/report-completion tests, and cross-surface security checks. The install handoff passes desktop, mobile, 320x568, and no-JavaScript WebKit coverage with responsive screenshots.
- Schema `0007` passes seven-migration up/down/up and all 11 isolated PostgreSQL integration tests. Staging retained a root-only schema `0006` checkpoint at `2026-07-21 21:28:35.255405+00`, LSN `0/3B921B8`, applied migration `0007` through the exact-host encrypted direct-endpoint runner, and reports `0007` ready. Public `@fullwell/fullwell@1.1.0` was published at `2026-07-21T22:04:36.658Z` with SHA-1 `35f24c6dabf1770c00a3fb9387e2fbd982b85b05` and SHA-512 integrity `sha512-LJqvdhclh+aPfjNYlH9scDLau2S4Fx6AzX4ZgWOcgi3+RyMIFYczdbmJclG0jmq54UMHTblfkNGemsRjEF01ww==`; `latest` resolves to `1.1.0`, and the downloaded registry artifact passes isolated Codex and Claude lifecycle tests.
- The aggregate deterministic suite passes 273 tests with 11 database-gated skips that pass separately in Apple Container. Coverage passes at 96.61% statements/lines, 94.88% functions, and 90.07% branches; the new onboarding domain, contract schemas, and install route each have 100% statement and branch coverage.
- The onboarding screencast preflight failed early because this macOS host has no X display `:99` or PulseAudio monitor required by the repository helper. No MP4 was produced; Playwright screenshots and interaction assertions are the available visual evidence.
- Direct WhatsApp gateway tests pass for raw-body HMAC verification, bounded text/delivery parsing, unsupported event accounting, two-sided same-browser linking, provider retry deduplication, per-link/global capacity, lease/heartbeat/completion, ambiguity resume, delivery status, revocation, retention cleanup, free-window enforcement, and the hard October 1 cutoff. The server gateway imports no LLM or retailer-control dependency.
- Schema `0006` passes migration up/down/up. The PostgreSQL messaging integration exercises timestamp normalization, pending/confirmed linking, deduplication before capacity, queue gauges, claim/heartbeat, follow-up resume, delivery updates, retention cleanup, and revocation. Database integration files run serially because the shared disposable operational-store fixture truncates tenant tables.
- The local runner passes OAuth PKCE, refresh rotation, Keychain-port, config, stable Node 24 selection, secret-free LaunchAgent, validated/atomic fixed-path snapshot, gateway error, Codex/Claude protocol, timeout SIGTERM/SIGKILL, stale-HEAD, action-receipt, disconnect-purge, and end-to-end orchestration tests. Its Codex adapter uses `~/Projects/fullwell-isolated-project-env`, a separate `CODEX_HOME`, and a pre-turn check requiring exactly `node_repl` plus enabled Browser and Chrome plugins.
- Restocking package validation and cross-host evals pass for one clear historical leader, salted/unsalted historical ambiguity, catalog-only alternatives, recurrence/recency conflict, prompt injection, stale authorization, crash recovery to a baseline-plus-one target, cross-origin/checkout/subscription blocks, and no novel substitution.
- The local fake retailer passes desktop, mobile, and 320x568 WebKit for cart quantity, injected page instructions, cross-origin/checkout/subscription controls, duplicate requests, and availability changes. Its API and safety states also pass with JavaScript disabled; only the JS-dependent cart-button scenario is skipped there. The keyring-backed isolated Codex host, persistent exact-origin Browser Use policy, and actual noninteractive quantity-one plus duplicate-replay flow pass. Claude Code browser-control execution remains a separate manual gate.
- Account setup/pending/expired/linked/revoke states, exact `runner:messages` consent, privacy disclosure, and aggregate queue-age/runner-online metrics pass React, view-model, Fastify, and observability tests without rendering provider identifiers, food text, or cart history.
- `npm run lint`, workspace typechecks, and workspace builds pass on the available Node 26.2.0 host; the production Docker build passes on pinned Node 24.1.0.
- Deterministic unit, component, application, load, and security coverage passes the enforced repository gate: 266 tests pass; 10 PostgreSQL integration tests skip when no database URL is supplied and pass through the isolated Apple Container gate. The last coverage run reported 96.61% statements/lines, 94.69% functions, and 90.06% branches.
- Coverage excludes process composition roots and Neon integration adapters from the deterministic unit denominator. Process roots remain covered by deployment/browser smoke checks; the Neon adapters remain covered by the isolated PostgreSQL integration gate.
- Neon integration passes all nine tests against isolated PostgreSQL 17 and a one-day schema-only Neon PostgreSQL 18 branch, including transaction-scoped household lock serialization, passkey public-key/counter persistence, identity ownership, final-owner safety, OAuth token revocation, one-time export claims, and bounded operator-health aggregation.
- All five reversible migrations pass up/down/up against the isolated local database.
- The fail-closed direct-Neon runner applied all five migrations to the disposable branch, recorded their content hashes, and applied zero on immediate re-entry. Exact-host, direct-endpoint, TLS, production-confirmation, transaction-lock, ordering, drift, and transaction-envelope behavior pass deterministic runner tests.
- Neon Console restored the disposable schema-only branch to its verified 2026-07-16 11:28 PDT history point in 0.26 seconds while retaining the prior state as an undo branch. The restored branch exposed the complete operational schema and migration-ledger versions `0001` through `0005`; both disposable branches have confirmed July 17 expiration. This proves the managed one-day PITR mechanism, not the required production 30-day retention/snapshot policy or a combined Git-plus-Neon RPO/RTO drill.
- Discoverable passkey registration, sign-in, pending-intent restoration, replay/binding rejection, account listing/removal, unsupported-browser states, and SimpleWebAuthn policy/error handling pass deterministic service, route, provider, React, memory-store, and PostgreSQL tests.
- Browser account lifecycle passes service, route, React, Git-fake, memory-store, and PostgreSQL tests for rename, browser-bound Apple/email linking, method removal, grant revocation, Git-audited leave, final-owner rejection, deletion, and former-member pseudonymization.
- Durable reconciliation passes deterministic and real PostgreSQL tests for post-commit projection failure, one-commit request-trailer replay, per-file projection revisions, membership repair, equal-head content drift, abandoned pre-commit cleanup, unsafe-repository quarantine, deterministic capability recovery, provisioning retry, and transaction-scoped account leave without nested-lock deadlock.
- Readable ZIP and full Git bundle delivery pass service, HTTP, account-route, React, Git, artifact-store, cleanup-worker, concurrency, expiry, and PostgreSQL tests. Downloads are content-hash verified, requester-bound, single-use, capped at 96 MiB, and backed by short-lived private files.
- Fail-closed global/grouped rate limits, trusted-proxy client keys, stable 429 responses, server-generated request IDs, structured redaction, pseudonymous identifiers, OpenMetrics output, dedicated operator authentication, production readiness, degraded health, reconciliation backlog, backup-gap, and volume-capacity reporting pass deterministic and real PostgreSQL tests.
- Backup cryptography and recovery pass deterministic tests for Ed25519 manifest signing, authenticated JWE encryption, object-type/key binding, ciphertext confidentiality, compliance-retention failure, checkpoint integrity, altered-content rejection, and real isolated Git bundle restore with HEAD/object-count/fsck verification. A generated SSH signing identity signs and verifies real commits through the production Git configuration, and the real AWS SDK adapter passes SigV4 PUT/HEAD/GET against a local S3-compatible boundary. PostgreSQL integration covers backup, repository-verification, restore-drill persistence, and schema `0005` aggregation.
- A live Backblaze B2 probe uploaded, headed, and downloaded an object under the production adapter's exact S3 configuration with 35-day compliance retention and a bucket/prefix-restricted key that lacks `deleteFiles` and `bypassGovernance`. Backblaze maps deletion by name to a hide marker under `writeFiles`; the compliance-retained upload remained listed and downloadable by native file ID.
- Backblaze returned `501 NotImplemented` for `If-None-Match: *`, so it cannot satisfy OpenTofu's native S3 lockfile contract. The stack now uses OpenTofu's Neon PostgreSQL backend, which provides database advisory locking through a direct connection.
- The dedicated Neon OpenTofu database, role, and staging schema initialized successfully; workspace create/delete exercised state writes and advisory locking. The DigitalOcean stack validates with a pinned 1 GiB Droplet, 50 GiB volume, weekly backups, and 2 GiB low-swappiness host swap, with no paid resource created before plan review.
- The reviewed DigitalOcean staging stack is live at `https://fullwell.souschefstudio.com` on a 1 GiB Droplet with the retained 50 GiB volume and explicit weekly backup policy. Encrypted systemd credentials start an unprivileged UID/GID `10001` container through a private runtime directory, `/health/live` and `/health/ready` return 200 through valid Caddy TLS, schema `0006` and Neon are ready, and deployed HTTP/MCP smokes pass.
- Before the WhatsApp staging migration, the operator recorded a Neon checkpoint at `2026-07-20T23:05:39.246Z`, LSN `0/3345500`, and schema `0005` in a root-only host record. The exact-host direct-endpoint runner then applied migration `0006`; the rollback SQL remains available, but no down-migration was attempted against staged messaging tables.
- The first staged WhatsApp image failed readiness because deployed operator health still reported hard-coded schema `0005`; systemd rollback restored the prior OAuth image. Staging now runs `hfj-staging:fullwell-20260721-8-runtime`; its Linux/amd64 OCI index digest is `sha256:0c482994f584dc3c7836111e914bccaad240f8f5c82b6299cb830901a4973558` and the checksum-matched transferred archive SHA-256 is `ec9125a4d65803e74cae015582c5a7bf1d8c41efeff416666348c0f3cd5d2b69`. Authenticated operator health reports schema `0007`, ready Git/volume/signing/single-writer checks, healthy reconciliation/repository/backup aggregates, and the pre-existing single `response_ready` message blocked on Meta approval.
- After the `fullwell-20260721-8` deployment, `npm run test:deploy-smoke -- staging`, `npm run test:mcp-smoke -- staging`, and the encrypted-credential non-destructive messaging smoke passed against the public HTTPS origin. Webhook intake, linking, and runner claims retain their previously verified enabled state; service replies remain disabled pending Meta approval, and no live retailer action was authorized.
- The non-destructive messaging smoke passed valid challenge, invalid token, unsigned body, and correctly signed empty body cases. Meta retained the verified callback and `messages` v25.0 subscription, the app was published, one real signed provider-identity inbound passed, and the authenticated two-sided sender/device link completed. No real in-window reply/delivery or provider retry/deduplication claim is made.
- Native Safari created a non-empty staging household through the authenticated, CSRF-protected `POST /households` boundary at 2026-07-20 00:00 UTC. The deployed maintenance timer verified its signed repository, uploaded an encrypted Git bundle and encrypted signed manifest to Backblaze, confirmed compliance retention through 2026-08-24 before persisting the checkpoint, and recorded no backup gap. The explicit restore command then downloaded and decrypted both objects, verified checkpoint and manifest hashes, household/HEAD identity, object count, fsck, and every commit signature in an isolated repository. Neon recorded a successful restore at 2026-07-20 00:01 UTC, and operator health reported zero backup gaps, zero fsck/signature failures, and `restore_drill_healthy: true`.
- Native Safari through Computer Use completed a real Apple authorization and cross-site `form_post` callback, reached the authenticated household view, linked an email identity through a delivered Resend message, signed out, requested a second one-time message, and completed an independent magic-link login. The live fix uses a bounded first-authorization payload, a short-lived `Secure; SameSite=None` binding cookie, and CSP `form-action` limited to self plus Apple's authorization origin.
- Native Safari 26.5 through Computer Use completed Touch ID passkey enrollment on `fullwell.souschefstudio.com`, displayed one enrolled passkey, signed out, and completed passkey-only sign-in to `/households`. The corresponding Neon credential remained active and recorded a later `last_used_at`; the deployed fix preserves SimpleWebAuthn's optional registration `hints` and `extensions.credProps` fields through the strict React boundary.
- Contract, dual-host packaging, 30-case Codex/Claude eval, security-boundary, and isolated Git bundle restore suites pass. The adversarial matrix covers authorization/scope, CSRF and redirect handling, replay/reuse, Git/path/archive rejection, races, bounded malformed/unsupported/oversized request failures, capability-safe route telemetry, XSS escaping, prompt-like public content as data, HTTP(S)-only browser URLs, recognizable repository-secret scanning across tracked and untracked files, and absence of server environment access from browser source; production browser source maps are disabled.
- Codex CLI 0.144.4 and Claude Code 2.1.123 discover the repository catalogs from `.agents/plugins/marketplace.json` and `.claude-plugin/marketplace.json`. Isolated local-marketplace tests pass Codex install/remove/reinstall and Claude install/update/disable/re-enable/uninstall without modifying user host state; the official Claude package validator also passes.
- The public immutable `@fullwell/household-food-journal-agent@1.0.0` package was published at 2026-07-20 01:56 UTC. Registry metadata reports public access, `latest` at `1.0.0`, 15 files, SHA-1 `d2d91305d9d5cd1f863bbbabddcc6b21a243cc9a`, and the same SHA-512 integrity prepared before publication. A clean public-registry install passes on pinned Node 24 in Apple Container, and the downloaded artifact passes isolated Codex install/remove/reinstall plus Claude install/update/disable/re-enable/uninstall.
- The replacement public package `@fullwell/fullwell@1.0.0` was published at 2026-07-20 06:13 UTC without changing the `household-food-journal` host plugin or MCP service identifiers. Registry metadata reports public access, 15 files, SHA-1 `ab265c4b264fe25248e2aff7c2a0a2a1f004f2cc`, and SHA-512 integrity `sha512-a9ZOY6cbP3EmOf8y+2Kr3TQBnyFZVXOeJ6K1dXi6WmQ8U6eEnQIT3UPreBg3BoY2TCNBw4hACMAlfpj0DuH30A==`; clean Node 24 installation in Apple Container and isolated Codex and Claude lifecycle tests against the downloaded registry artifact pass. The prior `@fullwell/household-food-journal-agent@1.0.0` artifact remains installable but now reports `Renamed to @fullwell/fullwell` as its npm deprecation notice.
- Codex CLI 0.144.4 completed live dynamic registration, native Safari consent, authorization-code exchange with a repeated MCP resource indicator, and clean reconnect against staging from an isolated host profile. Claude Code 2.1.215 completed the same flow, then sent initialize and the no-ID initialized notification; `claude mcp list` reported the deployed server connected after tool discovery.
- Native Safari grant management exercised the deployed revocation boundary after fresh passkey authentication. Ten duplicate grants from callback retries were removed, Claude changed to `Needs authentication`, and clean host logins created exactly one Codex and one Claude grant. A final revoke/reconnect repeated both host ceremonies after consent client-name binding was hardened. The deployed image was `hfj-staging:oauth-20260720-6-runtime`; the transferred OCI archive SHA-256 was `9c1cebea7cdbe88315c217dcc2729c9b39319a6bd1435f78bb0831c86e60d8c1` and the retained rollback image was `hfj-staging:oauth-20260720-5-runtime`.
- The Computer Use native pipe was unavailable for the final OAuth ceremony. Native Safari was instead operated through its macOS accessibility tree; accessibility press actions were unreliable for these server-rendered forms, while focusing the exact button and sending Return completed the POST. Callback completion, host status, and Account grant state provide the acceptance evidence rather than the click alone.
- WebKit passes 29 end-to-end checks with seven intentional project-specific skips across desktop Safari emulation, iPhone, 320x568, and no-JavaScript projects. WhatsApp setup/pending/linked Account states and the fake retailer are included. Axe reports no WCAG 2.0/2.1/2.2 A/AA violations across five live public routes and nine production-SSR screens; overflow, reduced motion, keyboard skip-link, account export, disclosure, and non-enumeration checks also pass.
- The deterministic load/race gate passes three scenarios in under one second: 100 concurrent MCP discoveries with unique request IDs, bounded non-enumerating preview shedding, 32-request idempotent fan-in producing one commit, 16 stale competitors producing one success and 15 conflicts, 50 cross-tenant reads revealing no household content, and 100 serialized same-household lock operations with cross-household progress.
- Safari on macOS was exercised through Computer Use: the Codex/Claude install selector updated through native accessibility controls and an invented collection capability displayed a non-enumerating unavailable state with no household fixture data.
- Safari also followed anonymous `/account` access to `/sign-in?returnTo=%2Faccount`, exposed labeled Apple/passkey/email and legal controls through accessibility APIs, preserved the pending-intent message, and displayed a strong visible email-field focus state without overlap.
- On macOS 26.5.1 with Safari 26.5, Computer Use confirmed native page zoom at exactly 200 percent. Install, sign-in, the long Privacy page, and the invalid-collection error state remained readable in a 1024x768 window without visible overlap or clipping. `Tab` reached form controls, Safari's `Option-Tab` reached links, and activating the skip link moved focus to main content; zoom was restored to 100 percent afterward.
- A 2026-07-15 Computer Use attempt to navigate native Safari to a temporary export-screen preview was inconclusive because the bridge replaced the typed localhost destination with `about:blank`; no export-screen native Safari claim is made from that attempt. The four WebKit export checks pass, while the manual accessibility review remains blocking.
- The repository screencast helper was attempted for the account export workflow, but the Homebrew macOS ffmpeg build rejected its required Linux `x11grab` input with exit code 234. No screencast artifact was produced; WebKit screenshots and interaction assertions provide the available local visual evidence.
- The same helper was attempted for `artifacts/screencasts/whatsapp-local-restocking.mp4`; Homebrew FFmpeg 8.0.1 again rejected `x11grab` and exited with code 234. No WhatsApp workflow MP4 was produced, so visible screencast acceptance remains blocked.
- Local deployment and MCP discovery smoke pass at `http://127.0.0.1:4187`.
- Docker image build, Compose render with credential files, Caddy validation, and OpenTofu validation pass.
- `npm audit --omit=dev` reports zero production vulnerabilities.

## Blocking evidence still required

- Complete a user-directed current-host snack-and-recipe conversation through explicit confirmation and retain a sanitized one-final-write transcript. The automated live smoke stops before mutation because inventing onboarding answers would alter the user's household; server atomicity, replay, recovery, and no-intermediate-write behavior already pass deterministic and PostgreSQL tests.

- A dedicated Meta developer app and employee system user have least-privilege access to the approved WABA. All seven WhatsApp values are installed as encrypted staging credentials, the WhatsApp-aware unit is active, schema `0006` and authenticated operator health pass, the HTTPS callback is verified, only `messages` v25.0 is subscribed, the app is published, and one real provider-identity inbound plus two-sided link passed. No real reply/delivery, provider retry, or token-rotation claim is made yet.
- The isolated Codex home now uses macOS Keychain without an `auth.json` credential file, and its supported global Browser Use policy allows exactly `http://127.0.0.1:4191`. A fresh noninteractive Codex process completed the fake-retailer flow with quantity `1` and an unchanged duplicate replay. Claude Code must still complete the same fixture flow, and one real retailer add-to-cart without checkout requires separate explicit authorization. Cowork remains unsupported as an inbound Fullwell runner API.
- Messaging staging load, secret rotation, encrypted retention deletion, cutoff, drain/rollback, provider outage, and billing review evidence remains required before runner claims or service replies are enabled. All five rollout gates are disabled.
- Public npm installation, isolated host lifecycle behavior, and real Codex/Claude OAuth revocation and reconnect now pass. A reviewed public host-catalog destination plus setup, invite, collection sharing/revocation, selective import, and canonical-data-preservation staging ceremonies remain blocked.
- Production Neon 30-day history/scheduled snapshots and the combined Git-plus-Neon RPO/RTO drill remain unproven; the deployed encrypted Backblaze recovery, Neon forward migration, adapter interoperability, and one-day point-in-time restore pass.
- Neon production retention/snapshot configuration plus DigitalOcean volume failover, secret-rotation, and production rollback checks require additional non-production credentials, data, and exercises. The staging host credential key is not backed by encrypted root media and is not accepted as the production secret-management posture.
- Provisioned latency/soak/resource-saturation testing plus OAuth token, collection import, Neon lock-wait, and live maintenance-overlap load remain required on staging.
- External staging security review, live provider-boundary probing, and secret-rotation/recovery exercises remain required; the deterministic adversarial matrix is not a penetration test.
- The manual compatibility, privacy, accessibility, and launch checklists remain no-go until every blocking row is complete; accessibility still requires VoiceOver, authenticated keyboard and zoom journeys, iPhone hardware, long authenticated/partial-success states, and a non-Apple screen-reader pairing.
